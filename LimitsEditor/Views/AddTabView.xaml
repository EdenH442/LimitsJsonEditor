<UserControl x:Class="LimitsEditor.Views.AddTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" UniformCornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Margin="0,0,10,0"
                         materialDesign:HintAssist.Hint="JSON file path"
                         Text="{Binding SelectedFilePath, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="1"
                        Padding="14,6"
                        Command="{Binding BrowseFileCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FolderSearchOutline" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="Browse" />
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Padding="16" UniformCornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Margin="0,0,12,0"
                         materialDesign:HintAssist.Hint="Sequence Name"
                         Text="{Binding SequenceName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBox Grid.Column="1"
                         Margin="0,0,12,0"
                         materialDesign:HintAssist.Hint="Test Name"
                         Text="{Binding TestName, UpdateSourceTrigger=PropertyChanged}" />

                <ComboBox Grid.Column="2"
                          materialDesign:HintAssist.Hint="Test Type"
                          ItemsSource="{Binding AvailableTestTypes}"
                          SelectedItem="{Binding TestType}" />
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="4" Padding="16" UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ListBox BorderThickness="0" ItemsSource="{Binding TestValues}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="0,0,0,8" Padding="10" UniformCornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBox Grid.Column="0" Margin="0,0,8,0" materialDesign:HintAssist.Hint="ResultType" Text="{Binding ResultType, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Column="1" Margin="0,0,8,0" materialDesign:HintAssist.Hint="ExpectedResult" Text="{Binding ExpectedResult, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Column="2" Margin="0,0,8,0" materialDesign:HintAssist.Hint="Comparison" Text="{Binding Comparison, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Column="3" Margin="0,0,8,0" materialDesign:HintAssist.Hint="Min" Text="{Binding Min, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox Grid.Column="4" Margin="0,0,8,0" materialDesign:HintAssist.Hint="Max" Text="{Binding Max, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="5"
                                            Padding="8,4"
                                            Command="{Binding DataContext.RemoveTestValueCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16" />
                                    </Button>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <CheckBox Margin="0,0,18,0"
                              VerticalAlignment="Center"
                              IsChecked="{Binding OverwriteExisting}"
                              Content="Overwrite existing test" />

                    <Button Margin="0,0,10,0"
                            Padding="14,6"
                            Command="{Binding AddTestValueCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusCircleOutline" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="Add Value" />
                        </StackPanel>
                    </Button>

                    <Button Padding="16,6"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding ApplyChangesCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSaveOutline" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="Apply Changes" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <TextBlock Grid.Row="6"
                   Opacity="0.75"
                   TextWrapping="Wrap"
                   Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>
