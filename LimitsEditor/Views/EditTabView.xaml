<UserControl x:Class="LimitsEditor.Views.EditTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" UniformCornerRadius="10">
            <TextBlock FontWeight="SemiBold"
                       Text="{Binding EditContextDescription, TargetNullValue=Select a limit from Find and click Edit.}" />
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Padding="16" UniformCornerRadius="10">
            <Grid DataContext="{Binding EditableLimit}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Multiple Step" />
                <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding MultipleStepNameCheck, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Limit Type" />
                <TextBox Grid.Row="2" Grid.Column="2" Text="{Binding LimitType, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Comparison" />
                <TextBox Grid.Row="4" Grid.Column="2" Text="{Binding ComparisonType, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Threshold Type" />
                <TextBox Grid.Row="6" Grid.Column="2" Text="{Binding ThresholdType, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="8" Grid.Column="0" Text="Expected" />
                <TextBox Grid.Row="8" Grid.Column="2" Text="{Binding ExpectedRes, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="10" Grid.Column="0" Text="Low" />
                <TextBox Grid.Row="10" Grid.Column="2" Text="{Binding Low, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="12" Grid.Column="0" Text="High" />
                <TextBox Grid.Row="12" Grid.Column="2" Text="{Binding High, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="14" Grid.Column="0" Text="Unit" />
                <TextBox Grid.Row="14" Grid.Column="2" Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
